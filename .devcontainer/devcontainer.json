{
    "name": "Puddle",
    "workspaceFolder": "/workspaces/puddle",
    "dockerFile": "Dockerfile",
    "settings": {
      "terminal.integrated.profiles.linux": {
        "bash": {
          "path": "/bin/bash"
        }
      },
      "terminal.integrated.defaultProfile.linux": "bash"
    },
    "extensions": [
      "example.extension1",
      "example.extension2"
    ],
    "forwardPorts": [3000, 8000],
    "postCreateCommand": "source venv/bin/activate && echo 'Entorno virtual activado'",
    "runArgs": [
      "--user", "albertoduran202"
    ]
  }
  
